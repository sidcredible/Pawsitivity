export enum OrderStatus {
  PENDING    = "pending",
  ACCEPTED   = "accepted",
  ON_THE_WAY = "on the way",
  DELIVERED  = "delivered",
  REJECTED   = "rejected",
  CANCELLED  = "cancelled"
}
